# Руководство по регистрации и аутентификации для мобильного приложения

## 🎯 Рекомендуемый подход

Для мобильного приложения поиска друзей в настольных играх рекомендуется **комбинированный подход**:

### 1. **Основной метод: Email/Password + JWT токены** ⭐
- Простая и надежная регистрация
- Хеширование паролей (bcrypt)
- JWT токены для аутентификации
- Refresh токены для безопасности

### 2. **OAuth интеграции** (рекомендуется добавить)
- **Google Sign-In** - популярный и удобный
- **Apple Sign-In** - обязательно для iOS приложений
- **Facebook** - опционально

### 3. **Телефонная верификация** (опционально)
- SMS верификация через сервисы (Twilio, AWS SNS)
- Полезно для повышения безопасности

## 🏗️ Архитектура аутентификации

### Flow регистрации:

```
1. Пользователь вводит email и пароль
   ↓
2. Бэкенд валидирует данные
   ↓
3. Хеширует пароль (bcrypt)
   ↓
4. Создает пользователя в БД
   ↓
5. Генерирует JWT токены (access + refresh)
   ↓
6. Отправляет токены клиенту
   ↓
7. Клиент сохраняет токены (SecureStorage)
   ↓
8. Использует access token для API запросов
```

### Flow авторизации:

```
1. Клиент отправляет access token в заголовке
   ↓
2. Бэкенд проверяет токен
   ↓
3. Извлекает user ID из токена
   ↓
4. Проверяет, что пользователь активен
   ↓
5. Разрешает доступ к ресурсам
```

## 🔐 JWT токены

### Access Token
- **Срок жизни**: 15 минут - 1 час
- **Хранение**: В памяти приложения (не в постоянном хранилище)
- **Использование**: В заголовке `Authorization: Bearer <token>`

### Refresh Token
- **Срок жизни**: 7-30 дней
- **Хранение**: В SecureStorage (Keychain на iOS, Keystore на Android)
- **Использование**: Для обновления access token

## 📱 Для мобильного приложения

### iOS (Swift)
```swift
// Использование Keychain для хранения refresh token
import Security

// Сохранение токена
let tokenData = refreshToken.data(using: .utf8)!
let query: [String: Any] = [
    kSecClass as String: kSecClassGenericPassword,
    kSecAttrAccount as String: "refreshToken",
    kSecValueData as String: tokenData
]
SecItemAdd(query as CFDictionary, nil)

// Получение токена
let getQuery: [String: Any] = [
    kSecClass as String: kSecClassGenericPassword,
    kSecAttrAccount as String: "refreshToken",
    kSecReturnData as String: true
]
```

### Android (Kotlin)
```kotlin
// Использование EncryptedSharedPreferences
val masterKey = MasterKeys.getOrCreate(MasterKeys.AES256_GCM_SPEC)
val sharedPreferences = EncryptedSharedPreferences.create(
    "auth_prefs",
    masterKey,
    context,
    EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,
    EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM
)

// Сохранение токена
sharedPreferences.edit().putString("refresh_token", refreshToken).apply()

// Получение токена
val refreshToken = sharedPreferences.getString("refresh_token", null)
```

## 🔒 Безопасность

### 1. Хеширование паролей
- Используйте `bcrypt` с salt rounds >= 10
- Никогда не храните пароли в открытом виде

### 2. HTTPS обязателен
- Все запросы должны идти через HTTPS
- Используйте SSL pinning в мобильном приложении

### 3. Валидация на клиенте и сервере
- Клиент: для UX (быстрая обратная связь)
- Сервер: для безопасности (обязательно!)

### 4. Rate Limiting
- Ограничьте количество попыток входа (например, 5 попыток в 15 минут)
- Защита от брутфорса

### 5. Token Refresh
- Автоматическое обновление access token
- Ротация refresh токенов

## 📋 Шаги реализации

### Шаг 1: Установить зависимости
```bash
npm install jsonwebtoken
npm install cookie-parser  # Для refresh токенов в cookies (опционально)
```

### Шаг 2: Создать Auth Service
- Генерация JWT токенов
- Верификация токенов
- Обновление токенов

### Шаг 3: Создать Auth Routes
- POST /api/auth/register - Регистрация
- POST /api/auth/login - Вход
- POST /api/auth/refresh - Обновление токена
- POST /api/auth/logout - Выход
- POST /api/auth/verify-email - Верификация email (опционально)

### Шаг 4: Создать Auth Middleware
- Проверка access token
- Извлечение user ID из токена
- Добавление user в request

### Шаг 5: Защитить роуты
- Применить middleware к защищенным роутам
- Пользователь должен быть авторизован для доступа

## 🚀 OAuth интеграции

### Google Sign-In
1. Создать проект в Google Cloud Console
2. Настроить OAuth 2.0 credentials
3. Получить client ID и client secret
4. Интегрировать в бэкенд (passport-google-oauth20)

### Apple Sign-In
1. Настроить в Apple Developer Console
2. Создать Service ID
3. Интегрировать в бэкенд
4. Обязательно для iOS приложений в App Store

## 📧 Email верификация (опционально)

### Использование сервисов:
- **SendGrid** - популярный email сервис
- **AWS SES** - дешевый вариант
- **Nodemailer** - для собственного SMTP

### Flow:
1. Пользователь регистрируется
2. Отправляется email с verification link
3. Пользователь переходит по ссылке
4. Бэкенд активирует аккаунт

## 📱 UX рекомендации

### Регистрация:
1. **Минимальный набор данных** на первом шаге (email, password)
2. **Постепенное заполнение профиля** после регистрации
3. **Валидация в реальном времени** (проверка email, силы пароля)
4. **Показ прогресса** регистрации

### Авторизация:
1. **Биометрическая аутентификация** (Face ID, Touch ID, Fingerprint)
2. **"Запомнить меня"** опция
3. **Автоматический вход** при наличии refresh token
4. **Обработка истечения токена** - автоматическое обновление

### Обработка ошибок:
- Понятные сообщения об ошибках
- Не показывать технические детали пользователю
- Логирование ошибок на сервере

## 🎨 Пример UI/UX Flow

```
Экран 1: Приветствие
   ↓
[Кнопка "Начать"]
   ↓
Экран 2: Выбор метода входа
   ↓
[Email/Password] [Google] [Apple]
   ↓
Экран 3: Форма регистрации/входа
   ↓
[Email] [Password] [Продолжить]
   ↓
Экран 4: Заполнение профиля (постепенно)
   ↓
[Имя] [Возраст] [Город] [Геолокация]
```

## ✅ Чеклист реализации

- [ ] JWT токены (access + refresh)
- [ ] Регистрация через email/password
- [ ] Авторизация через email/password
- [ ] Auth middleware для защиты роутов
- [ ] Автоматическое обновление токенов
- [ ] Обработка истечения токенов
- [ ] Rate limiting для защиты от брутфорса
- [ ] Валидация данных на клиенте и сервере
- [ ] Безопасное хранение токенов в мобильном приложении
- [ ] HTTPS обязателен
- [ ] (Опционально) OAuth Google
- [ ] (Опционально) OAuth Apple
- [ ] (Опционально) Email верификация
- [ ] (Опционально) SMS верификация

## 📚 Дополнительные ресурсы

- [JWT.io](https://jwt.io/) - Документация JWT
- [OWASP Mobile Security](https://owasp.org/www-project-mobile-security/) - Руководство по безопасности
- [RFC 6749](https://tools.ietf.org/html/rfc6749) - OAuth 2.0 спецификация

