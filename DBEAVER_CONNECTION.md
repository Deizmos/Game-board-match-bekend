# Подключение к базе данных через DBeaver

## 📋 Параметры подключения

Для подключения к базе данных `game_board_match` через DBeaver используйте следующие параметры:

### Основные параметры:
- **Host**: `localhost`
- **Port**: `5432`
- **Database**: `game_board_match`
- **Username**: `postgres`
- **Password**: `postgres`

## 🔧 Пошаговая инструкция

### Шаг 1: Убедитесь, что контейнер запущен

```bash
npm run db:up
```

Или проверьте статус:
```bash
docker-compose ps
```

### Шаг 2: Откройте DBeaver

1. Запустите DBeaver
2. Нажмите на кнопку **"New Database Connection"** (или `Cmd+N` / `Ctrl+N`)

### Шаг 3: Выберите PostgreSQL

1. В списке баз данных выберите **PostgreSQL**
2. Нажмите **Next**

### Шаг 4: Заполните параметры подключения

В открывшемся окне заполните следующие поля:

#### Основные настройки (Main):
- **Host**: `localhost`
- **Port**: `5432`
- **Database**: `game_board_match`
- **Username**: `postgres`
- **Password**: `postgres` ✅ (поставьте галочку "Save password" если хотите сохранить пароль)

#### Настройки драйвера (Driver properties):
Обычно настройки по умолчанию работают, но можно проверить:
- **Driver**: PostgreSQL (должен быть установлен автоматически)
- Если драйвера нет, DBeaver предложит скачать его автоматически

### Шаг 5: Тест подключения

1. Нажмите кнопку **"Test Connection"** внизу окна
2. Если всё настроено правильно, вы увидите сообщение: **"Connected"**
3. Если появится ошибка, убедитесь что:
   - Docker контейнер запущен (`docker-compose ps`)
   - Порт 5432 не занят другим приложением
   - Параметры подключения указаны правильно

### Шаг 6: Сохраните подключение

1. Нажмите **"Finish"**
2. Подключение появится в списке баз данных в левой панели

## 📊 Просмотр данных

После подключения вы сможете:

1. **Просмотреть все таблицы**:
   - Разверните базу данных `game_board_match`
   - Разверните папку `Schemas` → `public` → `Tables`
   - Вы увидите все таблицы: `users`, `profiles`, `games`, `user_games`, `matches`, `likes`, `messages`

2. **Просмотреть данные таблиц**:
   - Правый клик на таблице → **"View Data"**
   - Или двойной клик на таблице

3. **Выполнить SQL запросы**:
   - Правый клик на базе данных → **"SQL Editor"** → **"New SQL Script"**
   - Напишите SQL запрос и выполните его (F5 или кнопка "Execute")

## 🔍 Примеры SQL запросов

```sql
-- Просмотреть всех пользователей
SELECT * FROM users;

-- Просмотреть пользователей с профилями
SELECT u.id, u.name, u.email, p.bio, p.city, p.age
FROM users u
LEFT JOIN profiles p ON u.id = p."userId";

-- Просмотреть игры
SELECT * FROM games;

-- Просмотреть связь пользователей с играми
SELECT u.name, g.name as game_name, ug.skill_level, ug.is_favorite
FROM user_games ug
JOIN users u ON ug."userId" = u.id
JOIN games g ON ug."gameId" = g.id;

-- Подсчитать игры по категориям
SELECT category, COUNT(*) as count
FROM games
GROUP BY category;
```

## ⚠️ Возможные проблемы

### Проблема: "Connection refused" или "Connection timeout"

**Решение:**
1. Убедитесь, что контейнер запущен:
   ```bash
   docker-compose ps
   ```
2. Если контейнер не запущен, запустите его:
   ```bash
   npm run db:up
   ```

### Проблема: "Authentication failed"

**Решение:**
- Проверьте правильность пароля: `postgres`
- Убедитесь, что username указан как `postgres`

### Проблема: "Database does not exist"

**Решение:**
1. Убедитесь, что имя базы данных указано правильно: `game_board_match`
2. Проверьте, что миграции выполнены:
   ```bash
   npm run prisma:migrate
   ```

### Проблема: Драйвер PostgreSQL не найден

**Решение:**
1. DBeaver автоматически предложит скачать драйвер при первом подключении
2. Или вручную: **Database** → **Driver Manager** → **PostgreSQL** → **Download**

## 💡 Полезные советы

1. **Сохраните подключение**: DBeaver сохранит настройки подключения для быстрого доступа в будущем

2. **Используйте SQL Editor**: Для выполнения сложных запросов используйте встроенный SQL Editor

3. **Экспорт данных**: Можно экспортировать данные в различные форматы (CSV, JSON, Excel) через правый клик на таблице → **"Export Data"**

4. **Визуализация схемы**: DBeaver может показать ER-диаграмму базы данных через правый клик на базе данных → **"View Diagram"**

## 🔗 Альтернативные способы подключения

Если DBeaver не работает, можно использовать:
- **Prisma Studio**: `npm run prisma:studio`
- **psql**: `docker exec -it game-board-match-db psql -U postgres -d game_board_match`
- **pgAdmin**: Альтернативный GUI для PostgreSQL

