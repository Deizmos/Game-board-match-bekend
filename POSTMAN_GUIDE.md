# Руководство по тестированию API через Postman

## 🚀 Быстрый старт

### Шаг 1: Запустите сервер

```bash
npm run dev
```

Сервер будет доступен на `http://localhost:3000`

### Шаг 2: Убедитесь, что база данных запущена

```bash
npm run db:up
```

## 📝 Примеры запросов для Postman

### 1. Регистрация пользователя

**Метод:** `POST`  
**URL:** `http://localhost:3000/api/auth/register`

**Headers:**
```
Content-Type: application/json
```

**Body (raw JSON):**
```json
{
  "email": "test@example.com",
  "password": "password123",
  "name": "Тестовый пользователь",
  "profile": {
    "bio": "Люблю настольные игры",
    "age": 25,
    "city": "Москва",
    "latitude": 55.7558,
    "longitude": 37.6173
  }
}
```

**Минимальный запрос (без профиля):**
```json
{
  "email": "test2@example.com",
  "password": "password123",
  "name": "Тест"
}
```

**Ожидаемый ответ (201 Created):**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "email": "test@example.com",
      "name": "Тестовый пользователь",
      "profile": {
        "id": 1,
        "bio": "Люблю настольные игры",
        "age": 25,
        "city": "Москва",
        "latitude": 55.7558,
        "longitude": 37.6173
      }
    },
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  },
  "message": "Регистрация прошла успешно"
}
```

---

### 2. Вход пользователя

**Метод:** `POST`  
**URL:** `http://localhost:3000/api/auth/login`

**Headers:**
```
Content-Type: application/json
```

**Body (raw JSON):**
```json
{
  "email": "test@example.com",
  "password": "password123"
}
```

**Ожидаемый ответ (200 OK):**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "email": "test@example.com",
      "name": "Тестовый пользователь"
    },
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  },
  "message": "Вход выполнен успешно"
}
```

---

### 3. Получить текущего пользователя (требуется авторизация)

**Метод:** `GET`  
**URL:** `http://localhost:3000/api/auth/me`

**Headers:**
```
Authorization: Bearer <ваш-access-token>
Content-Type: application/json
```

**Пример:**
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjEsInR5cGUiOiJhY2Nlc3MiLCJpYXQiOjE3Mjk4NzYzMjAsImV4cCI6MTcyOTg3NzIyMH0.xxxxx
```

**Ожидаемый ответ (200 OK):**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "email": "test@example.com",
    "name": "Тестовый пользователь",
    "isActive": true,
    "profile": {
      "bio": "Люблю настольные игры",
      "age": 25,
      "city": "Москва"
    }
  }
}
```

---

### 4. Обновление токенов

**Метод:** `POST`  
**URL:** `http://localhost:3000/api/auth/refresh`

**Headers:**
```
Content-Type: application/json
```

**Body (raw JSON):**
```json
{
  "refreshToken": "ваш-refresh-token-здесь"
}
```

**Ожидаемый ответ (200 OK):**
```json
{
  "success": true,
  "data": {
    "accessToken": "новый-access-token",
    "refreshToken": "новый-refresh-token"
  },
  "message": "Токены обновлены"
}
```

---

### 5. Выход пользователя

**Метод:** `POST`  
**URL:** `http://localhost:3000/api/auth/logout`

**Headers:**
```
Authorization: Bearer <ваш-access-token>
Content-Type: application/json
```

**Body (raw JSON):**
```json
{
  "refreshToken": "ваш-refresh-token-здесь"
}
```

**Ожидаемый ответ (200 OK):**
```json
{
  "success": true,
  "message": "Выход выполнен успешно"
}
```

---

## 🔧 Настройка Postman

### Шаг 1: Создайте коллекцию

1. Откройте Postman
2. Нажмите **"New"** → **"Collection"**
3. Назовите коллекцию: **"Game Board Match API"**

### Шаг 2: Создайте переменные окружения

1. Нажмите на **"Environments"** в левом меню
2. Создайте новое окружение: **"Local Development"**
3. Добавьте переменные:
   - `base_url`: `http://localhost:3000`
   - `access_token`: (оставьте пустым, будет заполняться автоматически)
   - `refresh_token`: (оставьте пустым, будет заполняться автоматически)

### Шаг 3: Настройте автозаполнение токенов

После успешного входа или регистрации:

1. Откройте вкладку **"Tests"** в запросе регистрации/входа
2. Добавьте следующий код:

```javascript
// Сохраняем токены в переменные окружения
if (pm.response.code === 200 || pm.response.code === 201) {
    const jsonData = pm.response.json();
    if (jsonData.data && jsonData.data.accessToken) {
        pm.environment.set("access_token", jsonData.data.accessToken);
        pm.environment.set("refresh_token", jsonData.data.refreshToken);
        console.log("Токены сохранены в переменные окружения");
    }
}
```

3. Для запросов, требующих авторизации, используйте:
   - В заголовке `Authorization`: `Bearer {{access_token}}`

---

## 📋 Чек-лист тестирования

### ✅ Базовая регистрация
- [ ] Регистрация с email и паролем
- [ ] Регистрация с полным профилем
- [ ] Регистрация с геолокацией
- [ ] Проверка валидации (некорректный email)
- [ ] Проверка валидации (короткий пароль)
- [ ] Проверка дублирования email

### ✅ Вход
- [ ] Вход с правильными данными
- [ ] Вход с неправильным паролем
- [ ] Вход с несуществующим email

### ✅ Авторизация
- [ ] Получение текущего пользователя с токеном
- [ ] Попытка доступа без токена
- [ ] Попытка доступа с недействительным токеном

### ✅ Обновление токенов
- [ ] Обновление токенов с валидным refresh token
- [ ] Попытка обновления с недействительным токеном

---

## 🐛 Обработка ошибок

### Ошибка 400 (Bad Request)
```json
{
  "success": false,
  "error": {
    "message": "Ошибка валидации",
    "details": [
      {
        "msg": "Некорректный email адрес",
        "param": "email"
      }
    ]
  }
}
```

### Ошибка 401 (Unauthorized)
```json
{
  "success": false,
  "error": {
    "message": "Недействительный токен"
  }
}
```

### Ошибка 409 (Conflict)
```json
{
  "success": false,
  "error": {
    "message": "Пользователь с таким email уже существует"
  }
}
```

---

## 💡 Полезные советы

1. **Используйте переменные окружения** для `base_url` и токенов
2. **Сохраните токены автоматически** через скрипты в Tests
3. **Создайте отдельные запросы** для каждого endpoint
4. **Используйте Pre-request Scripts** для автоматической подстановки токенов
5. **Настройте авторизацию на уровне коллекции** для автоматического добавления токена

---

## 🔐 Пример Pre-request Script для автоматической авторизации

Добавьте в коллекцию в раздел **"Pre-request Script"**:

```javascript
// Автоматически добавляем токен в заголовки, если он есть
const accessToken = pm.environment.get("access_token");
if (accessToken) {
    pm.request.headers.add({
        key: "Authorization",
        value: "Bearer " + accessToken
    });
}
```

---

## 📱 Полный пример тестирования

### 1. Регистрация → Сохранение токенов
### 2. Получение текущего пользователя → Проверка авторизации
### 3. Обновление токенов → Проверка refresh механизма
### 4. Выход → Проверка деактивации токенов

Все готово для тестирования! 🎉

